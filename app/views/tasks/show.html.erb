<h1><%= @task.title %></h1>
<h3><%= @task.details %></h3>

<div>
  <% if @task.completed? %>
  <%= "This task is completed"%>
  <% else %>
  <%= "This task is not completed yet"%>
  <% end %>
</div>

<div>
  <%= link_to "Back to list", tasks_path %>
</div>
